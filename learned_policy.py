
######################################
# THIS FILE IS AUTOMATICALLY GENERATED FROM PIPS'S SOLUTION
# DO NOT EDIT 
######################################


def policy_ldips(state):
    x_diff = state.get("x_diff")
    v_diff = state.get("v_diff")
    acc = state.get("acc")
    #v_self = state.get("v_self")
    #v_front = state.get("v_front")


    ### PIPS LEARNED EXPRESSIONS
    slow_to_fast=((((v_diff) ** 2 / acc) + x_diff) > 29.234102249145508)
    fast_to_slow=((((x_diff - x_diff) - (x_diff + x_diff)) > -69.7161865234375) and (v_diff < 0.02183300070464611))
    slow_to_slow=((((v_diff) ** 2 / acc) - (x_diff + x_diff)) > -58.14498519897461)
    fast_to_fast=(v_diff > -0.13891200721263885)

    ###

    pre = state.get("start")
    if pre == "SLOWER":
        if slow_to_fast:
            post = "FASTER"
        elif slow_to_slow:
            post = "SLOWER"
        else:
            post = "SLOWER"
    elif pre == "FASTER":
        if fast_to_slow:
            post = "SLOWER"
        elif fast_to_fast:
            post = "FASTER"
        else:
            post = "FASTER"
    return post
